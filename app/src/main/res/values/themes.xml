<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MovieApp" parent="Base.Theme.MovieApp">
        <item name="selectableItemBackground">@android:color/transparent</item>
        <item name="shapeAppearanceSmallComponent">@style/Widget.MovieApp.ButtonShape</item>
        <item name="shapeAppearanceMediumComponent">@style/Widget.MovieApp.ButtonShape</item>
        <item name="shapeAppearanceLargeComponent">@style/Widget.MovieApp.ButtonShape</item>
    </style>

</resources>